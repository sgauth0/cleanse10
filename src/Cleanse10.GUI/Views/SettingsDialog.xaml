<Window x:Class="Cleanse10.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Settings"
        Width="560" Height="250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="44">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Margin="16,0,0,0"
                               VerticalAlignment="Center"
                               Text="Settings"
                               Style="{StaticResource H2}"/>

                    <Button Grid.Column="1"
                            Width="44"
                            Height="44"
                            Content="&#xE711;"
                            FontFamily="Segoe MDL2 Assets"
                            Style="{StaticResource TitleBarButton}"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="28,24,28,28">
                <TextBlock Text="Temporary mount workspace"
                           Style="{StaticResource H2}"
                           FontSize="12"
                           Margin="0,0,0,4"/>

                <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding MountRoot, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0"/>

                    <Button Grid.Column="1"
                            Content="Default"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding UseDefaultCommand}"
                            Margin="0,0,8,0"/>

                    <Button Grid.Column="2"
                            Content="Browse..."
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding BrowseMountRootCommand}"/>
                </Grid>

                <TextBlock Text="Cleanse10 creates a fresh mount subfolder here for each build, then cleans it up when the run finishes."
                           Style="{StaticResource Caption}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,6"/>

                <TextBlock Text="{Binding DefaultMountRoot, StringFormat=Default: {0}}"
                           Style="{StaticResource Caption}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,22"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding CancelCommand}"
                            MinWidth="80"
                            Margin="0,0,10,0"/>
                    <Button Content="Save"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}"
                            MinWidth="120"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
